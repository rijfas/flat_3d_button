name: flat_3d_button
description: A simple 3D flat button with click animation,
  flat 3d button contains 3 methods,
  a flat 3d button with any widget as it's child,
  text as it's child and also an icon as it's child
version: 1.0.1
homepage: https://github.com/rijfas/flat_3d_button
repository: https://github.com/rijfas/flat_3d_button

environment:
  sdk: ">=2.17.5 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
